{
  "name": "@sapsos/api",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "python -m app.run",
    "lint": "python -m ruff check .",
    "test": "python -m pytest",
    "typecheck": "python -m mypy .",
    "format": "python -m ruff format --check .",
    "openapi": "python -m app.openapi_export",
    "build": "python -m compileall app"
  }
}
